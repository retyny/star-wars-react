import React from 'react';

const Footer = () =>
{
    return (
        <footer className="rounded-bottom">
            <div>
                <p className="border border-light rounded-pill btn btn-danger">Send me an <span>email</span></p>
            </div>
        </footer>
    );
};

export default Footer;